<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head.ejs') %>
    <title>Puzzle</title>
</head>
<body>
    <%- include('../partials/nav.ejs') %>
    <div class="container-fluid text-center">
        <img src="https://i.imgur.com/iWw6nnu.png" width="200px">
    </div>
    <div class="container card mb-3 my-3 border-light shadow p-3 mb-5 bg-body rounded" style="max-width: 80%;">
        <div class="row g-0">
            <div class="col-md-4">
                <img src="<%=puzzle.img%>" class="img-fluid rounded-start" alt="...">
            </div>
            <div class="col-md-8">
                <div class="card-body d-flex flex-column" style="height: 100%;">
                    <h1 class="card-title"><%= puzzle.title %></h1>
                    <p class="card-text fs-4"><%= puzzle.description %></p>
                    <p class="card-text">
                        <small class="text-muted">
                        <% if (puzzle.isAvailable == true) { %>
                            <p>Currently Available</p>
                            <% } else { %>
                            <p>Someone is currently enjoying this puzzle!</p>
                        <% }; %>
                        </small>
                    </p>
                    <div class="mt-auto">
                        <form action="/<%= puzzle._id %>/add?_method=PUT", method="POST">
                            <button class="btn btn-primary">Add to Exchange Inventory</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>